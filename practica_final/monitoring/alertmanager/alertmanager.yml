route:
  receiver: default
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: default
    webhook_configs:
      - url: "http://api:8080/api/alerts/webhook"
        send_resolved: true
